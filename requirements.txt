# Runtime dependencies for terminschleuder-extractor.
# Prod runs in the container (Dockerfile installs these); the host venv
# installs these + requirements-dev.txt so tests run on the host.
#
# HTML parsing uses BeautifulSoup with the stdlib ``html.parser`` (no compiled
# C dependency), so no lxml is required — keeps the install clean on hosts
# without libxml2 dev headers (and on new Python versions without lxml wheels).
httpx>=0.27,<1.0
beautifulsoup4>=4.12,<5.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
structlog>=24.1,<26.0
openai>=1.40,<2.0